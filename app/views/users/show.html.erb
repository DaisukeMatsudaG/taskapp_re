<h3><%= @user.name %>さんのタスク一覧</h3>
<%= form_with model: @task do |f|%>
    <%= f.text_field :content %>
    <%= f.submit '投稿する' %>
<% end %>
<%= render 'commons/tasks' %>

<%#ページネーション用の記述%>
<%= paginate @tasks %>